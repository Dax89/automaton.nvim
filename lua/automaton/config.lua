return {
    debug = false,
    saveall = true,

    events = {
        workspacechanged = nil,
    },

    impl = {
        workspace = ".automaton",
        variablesfile = "variables.json",
        tasksfile = "tasks.json",
        launchfile = "launch.json",
    }
}
